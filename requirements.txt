# --- Core app framework ---
streamlit==1.37.0        # Web UI framework for building interactive dashboards and chat apps
openai==1.55.3           # Official OpenAI client library (latest 1.x release, compatible with langchain-openai)

# --- LangChain ecosystem (aligned on langchain-core 0.2.x) ---
langchain==0.2.14        # Main LangChain library (chains, agents, integrations)
langchain-core==0.2.38   # Core abstractions required by langchain + langchain-openai
langchain-openai==0.1.7  # LangChain wrapper for OpenAI models (embeddings + chat)
langchain-chroma==0.1.4  # LangChain integration with ChromaDB vector store
langsmith==0.1.75        # Required by langchain-core for tracing and debugging
langchain-text-splitters==0.2.4   # Utility for splitting text into chunks for RAG

# --- Vector database ---
chromadb==0.5.3          # ChromaDB vector database (modern version, matches langchain-chroma)
rpds-py==0.18.0          # Rust-based persistent data structures (dependency of ChromaDB)
faiss-cpu==1.8.0         # FAISS CPU-only build for vector similarity search

# --- Validation & utilities ---
pydantic==2.7.4          # Data validation and settings management (used by LangChain + app configs)
pydantic-core==2.18.4    # Core engine for Pydantic v2
tenacity==8.3.0          # Retry logic utility (used by LangChain + OpenAI clients)
packaging==24.1          # Version parsing and packaging utilities (dependency for multiple libs)

# --- Document handling ---
pypdf==4.2.0             # PDF parsing and text extraction for ingestion into RAG
markdown==3.7            # Markdown parsing/rendering for handling .md files
